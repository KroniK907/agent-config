# agent-config-wizard binaries

Linux and Windows builds of `agent-config-wizard` live here. CI builds them on every push to `main` and commits updates when the output changes.

Do not commit locally built copies. Build for local testing to the parent directory instead (see [../README.md](../README.md#build)).
